@@
-      - name: Set up JDK 17
-        uses: actions/setup-java@v4
-        with:
-          distribution: temurin
-          java-version: '17'
+      - name: Set up JDK 11
+        uses: actions/setup-java@v4
+        with:
+          distribution: temurin
+          java-version: '11'
@@
-      - name: Show Java, SDK & Gradle info
-        run: |
-          java -version
-          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT"
-          ls -la $ANDROID_SDK_ROOT || true
-          echo "--- Gradle Wrapper ---"
-          cat gradle/wrapper/gradle-wrapper.properties || true
-          ./gradlew --version || true
+      - name: Show Java, SDK & Gradle info
+        run: |
+          java -version
+          echo "ANDROID_SDK_ROOT=$ANDROID_SDK_ROOT"
+          ls -la $ANDROID_SDK_ROOT || true
+          echo "--- Gradle Wrapper ---"
+          cat gradle/wrapper/gradle-wrapper.properties || true
+          ./gradlew --version || true
+          echo "--- Save gradle build output to build.log ---"
+          ./gradlew assembleDebug --no-daemon --warning-mode=all --info --stacktrace > build.log || true
@@
-      - name: Clean
-        run: ./gradlew clean --warning-mode=none --stacktrace || true
-
-      - name: Assemble Debug APK (first: avoid signing issues)
-        run: ./gradlew assembleDebug --no-daemon --warning-mode=all --info --stacktrace || true
+      - name: Clean
+        run: ./gradlew clean --warning-mode=none --stacktrace || true
+
+      - name: Assemble Debug APK (first: avoid signing issues)
+        run: ./gradlew assembleDebug --no-daemon --warning-mode=all --info --stacktrace || true
@@
-      - name: Upload build logs
+      - name: Upload build logs
         if: always()
         uses: actions/upload-artifact@v4
         with:
           name: gradle-build-logs
           path: |
-            **/build.log
-            **/*build*/**/*.log
-            build/reports/**/*.html
+            build.log
+            **/*build*/**/*.log
+            build/reports/**/*.html
